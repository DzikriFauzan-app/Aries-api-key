{
  "name": "aries-api-key",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node -e \"require('fs').readdirSync('dist/tests').filter(f=>f.endsWith('.test.js')).sort().forEach(f=>{console.log('[TEST]',f); require('./dist/tests/'+f);})\"",
    "benchmark": "ARIES_BENCH=1 node dist/tests/benchmark.test.js",
    "ci": "npm run build && npm run test && npm run benchmark"
  },
  "dependencies": {
    "fastify": "^5.6.2",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.2"
  }
}

{
  "name": "aries-api-key",
  "version": "0.1.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "boot": "node dist/boot/boot_sovereign.js",
    "test": "node -e \"require('fs').readdirSync('dist/tests').filter(f=>f.endsWith('.test.js')).sort().forEach(f=>require('./dist/tests/'+f))\""
  }
}
